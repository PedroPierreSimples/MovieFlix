export type User = {
    id?: number,
    movieId?: number,
    author: string,
    email: string,
    rating?: number,
    reviewContent?: string,
    reviewDate?: string,
    watchedDate?: string
} 